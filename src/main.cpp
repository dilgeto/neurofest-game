#include <raylib.h>
#include <string>

int main() {
	InitWindow(1680,900, "NeuroGame");
	SetTargetFPS(60);
	
	while (!WindowShouldClose()) {
		// Mouse position
		Vector2 mousePosition = GetMousePosition();
		
		Rectangle button

		// Drawing
		BeginDrawing();
		EndDrawing();
	}

	return 0;
}
